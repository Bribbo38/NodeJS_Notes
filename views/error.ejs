<!DOCTYPE html>
<html lang="en">

<%- include('partials/head', {title: `${error.status || 500} - ${error.status ? error.message : 'Internal Error'}` }) %>

<body class="bg-base-300 flex flex-col min-h-screen">
    <%- include('partials/header') %>


    <main class="flex-grow flex items-center justify-center text-center min-h-screen">
        <div class="bg-base-100 p-7 rounded-lg shadow-lg max-w-sm mx-auto">
            <h1 class="text-4xl font-bold text-red-500"><%- `${error.status || 500} - ${error.status ? error.message : 'Internal Error'}` %></h1>
            <p class="text-lg text-base-content mt-2"><%= message %></p>
            <a href="/" class="mt-5 inline-block text-blue-500 hover:text-blue-700">Go back home</a>
        </div>
    </main>


    <%- include('partials/footer') %>
</body>

</html>